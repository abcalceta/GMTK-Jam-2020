extends "res://Scripts/LevelTextHandling.gd"


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	texts = [
	"This next cave looks a little flooded...",
	"As expensive as these machines are, they aren't waterproof...",
	"If anything, touching water kills them.",
	"There's not that much water right now, so this won't be that hard.",
	"Alright! Let's carefully get this machine into the dock.",
	]
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
